# 生產資訊中心 - 目錄結構說明

## 📁 正確的開發目錄

本應用程式使用以下目錄結構：

### ✅ 請在這些目錄進行開發

```
C:\app\order\
├── app/                          # 主應用程式目錄
│   ├── views/                    # ✅ HTML 模板文件 (正確位置)
│   │   ├── procurement.html
│   │   ├── order_query.html
│   │   ├── admin_dashboard.html
│   │   └── login.html
│   │
│   ├── static/                   # ✅ 靜態資源文件 (正確位置)
│   │   ├── css/
│   │   │   ├── style.css
│   │   │   └── pico.min.css
│   │   └── js/
│   │       ├── main.js
│   │       └── admin.js
│   │
│   ├── controllers/              # 控制器
│   ├── models/                   # 資料模型
│   ├── services/                 # 業務邏輯服務
│   ├── utils/                    # 工具函數
│   └── __init__.py               # 應用程式工廠
│
├── instance/                     # SQLite 資料庫
│   └── order_management.db
│
├── run.py                        # 應用程式啟動入口
└── requirements.txt              # Python 依賴套件
```

## ❌ 已備份的舊目錄（請勿使用）

以下目錄已重命名為備份，**不會被應用程式使用**：

- `_templates_backup_20251129/` - 舊的 templates 目錄（未使用）
- `_static_backup_20251129/` - 舊的 static 目錄（未使用）

## 🔍 為什麼有兩套目錄？

在 `app/__init__.py` 中，Flask 應用程式配置如下：

```python
app = Flask(__name__, 
            template_folder='views',      # 使用 app/views/
            static_folder='static')       # 使用 app/static/
```

因此：
- ✅ **app/views/** 是實際使用的模板目錄
- ✅ **app/static/** 是實際使用的靜態資源目錄
- ❌ **templates/** 和 **static/** 在根目錄不會被使用

## 📝 開發時注意事項

1. **修改前端代碼時**，請確認您正在編輯：
   - `app/views/*.html`
   - `app/static/js/main.js`
   - `app/static/css/style.css`

2. **修改後端代碼時**，請編輯：
   - `app/services/*.py`
   - `app/controllers/*.py`
   - `app/models/*.py`

3. **如果需要恢復備份**：
   - 備份目錄保留了之前的文件
   - 可以視需要參考或刪除

## 🗑️ 清理備份目錄

如果確認不再需要備份，可以執行：

```powershell
# 刪除備份目錄
Remove-Item -Path "_templates_backup_*" -Recurse -Force
Remove-Item -Path "_static_backup_*" -Recurse -Force
```

## 🚀 開發流程

1. 修改 `app/views/` 或 `app/static/` 中的文件
2. 重新啟動服務器（如果修改了 Python 代碼）
3. 清除瀏覽器快取（如果修改了前端代碼）
   - `Ctrl + Shift + R` 或 `Ctrl + F5`
4. 測試功能

## ⚠️ 重要提醒

**始終在 `app/` 目錄下進行開發，不要在根目錄的備份文件夾中修改文件！**

---

最後更新：2025-11-29
